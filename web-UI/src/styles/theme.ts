import { createTheme } from "@mui/material";
import "@fontsource/funnel-display/400.css";

const theme = createTheme({
  typography: {
    fontFamily: '"Funnel Display", "Roboto", "Helvetica", "Arial", sans-serif',
  },
});

export default theme;
